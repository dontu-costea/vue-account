import { createStore } from 'vuex'

export default createStore({
  state: () => ({
    defaultEmail: "dontu",
    defaultPassword: "costea",
    logged: false
  }),
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
